#

PREFIX=/vol/mgx-sw

OS=`uname -o`

GROUP=mgxserv


if [ ! -d ${PREFIX} ]; then
    install -d -m 02755 -g ${GROUP} ${PREFIX}
fi

if [ ! -d ${PREFIX}/bin ]; then
    install -d -m 02755 -g ${GROUP} ${PREFIX}/bin
fi

# EOF
